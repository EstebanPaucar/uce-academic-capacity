- name: Install Go Dependencies
        run: |
          if [ -f go.mod ]; then
            go mod download
          elif [ -f apps/backend/capacity-calculation-service/go.mod ]; then
            cd apps/backend/capacity-calculation-service && go mod download
          else
            echo "No go.mod found, skipping download."
          fi