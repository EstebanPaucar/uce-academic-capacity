name: CI - QA Development Flow

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  validate-infrastructure:
    name: Terraform Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: |
          cd terraform
          terraform init -backend=false

      - name: Terraform Validate
        run: |
          cd terraform
          terraform validate

  validate-apps:
    name: Build Microservices (Nx)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Necesario para que Nx pueda comparar cambios entre commits

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
          cache: true

      - name: Install Node Dependencies
        run: npm install

      - name: Install Go Dependencies
        # Buscamos archivos go.mod tanto en la raíz como en las subcarpetas de apps
        run: |
          find . -name "go.mod" -execdir go mod download \;
          find . -name "go.mod" -execdir go mod tidy \;

      - name: Build Affected Projects
        # Solo compila lo que cambió respecto a la rama develop
        run: npx nx affected -t build --base=origin/develop --no-cloud